package com.mc.coffeemanager.service;

public class Sales {
	
	public Payment takeOrder(Order order) {
		
		order.execute(account);
		Payment payment = new Payment(order, account);
		payment.excute();
		
		return payment;
	}

}
